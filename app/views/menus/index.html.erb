<p id="notice"><%= notice %></p>

<h1>Listing Menus</h1>
<div class="table-responsive">
  <table class="table">
    <%= render 'thead_form' %>
    <tbody>
    <%= render(@menus) %>
  </tbody>
  </table>
</div>
<br>

<%= link_to 'New Menu', new_menu_path %>

